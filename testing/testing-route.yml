apiVersion: v1
kind: Route
metadata:
  name: myapp
  namespace: testing
spec:
  host: myapp-testing.cloudapps.example.com
  to:
    kind: Service
    name: myapp
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None
