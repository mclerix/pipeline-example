kind: Service
metadata:
  name: myapp
  namespace: test
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    deployment-config.name: myapp
  type: ClusterIP
  sessionAffinity: None
status:
  loadBalancer: {}
